$('#playing-playlist').find('.ListHead.listnode-content').append("<%= escape_javascript(render(:partial => 'list_nodes/node',:locals => { :nodetree => @nodetree, :node_id => @node_id } )) %>");

$('.playlistchanged').trigger('playlistchanged');
$('#playing-playlist').playlist();
